<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiWrongBindTest.SecondPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="SecondPage">
    <VerticalStackLayout
        Padding="30"
        Spacing="25"
        VerticalOptions="Center">
        <Label HorizontalOptions="Center" Text="There's Binding failure on the second page and this one should appear in the XAML Binding Failures window." />
        <Label Text="{Binding Wrong.Name}" />
    </VerticalStackLayout>
</ContentPage>
